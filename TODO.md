# TODO

이번 실행에서는 "동작하는 최소 기능"을 우선했습니다. 아래는 이번 스코프에서
의도적으로 남겨둔 작업입니다.

## 테스트

- 단위/통합/E2E 테스트가 전혀 없습니다. `src/lib/progress.ts`(진행률 계산
  규칙)와 `src/app/api/**`의 요청 검증/활동 로그 기록 로직이 우선순위가
  높은 테스트 대상입니다.
- 수동으로는 dev 서버를 띄워 `/tasks`, `/tasks/new`, `/tasks/[id]`,
  `/calendar`(일/주/월/리스트), `/projects`와 관련 API를 curl로 스모크
  테스트했습니다 (생성/조회/상태변경/단계 토글/진행률 자동계산/삭제).

## 인증 / 멀티유저

- 로그인/세션이 없습니다. `src/lib/current-user.ts`가 이메일로 고정된
  단일 사용자를 조회/생성해서 모든 요청에 사용합니다. 실제 인증(NextAuth
  등)을 붙일 때 이 함수의 구현만 교체하면 되도록 분리해 두었습니다.
- `Task.ownerId` / `Project.ownerId`가 이미 있으므로 멀티유저 지원 자체는
  스키마 변경 없이 가능합니다.

## 권한 승인 대기로 스킵한 항목

무인 실행 규칙에 따라 시스템 설정 변경이 필요한 작업은 시도하지 않았습니다.
현재까지는 해당되는 항목이 없었습니다 (전부 저장소 내부 파일 작업으로 완료).

## v1.1 이후 로드맵 (이번 구현에 없음)

- **v1.1** 팀 공유/워크스페이스: `Workspace`, `Membership` 모델, 팀
  대시보드, 댓글/멘션. `Task.visibility`에 `TEAM_SHARED` 값은 이미
  존재하지만 UI는 비활성화 상태입니다.
- **v1.2** Google Calendar / Slack 연동, 알림. `/api/tasks`가 이미 REST로
  분리되어 있어 동기화 잡을 붙이기 쉬운 구조입니다.
- **v1.3** 실험데이터/논문원고 파일 첨부, 공유범위, 버전관리(diff/rollback).
- **v1.4** 주간보고서 통합, 주제별 그룹핑.
- **v2** 모바일 앱, 리포트 자동화, 원고 공동편집 고도화.
- **v2.1** AI 연구 히스토리 인사이트(베타), 근거 출처 표시, 피드백 루프.

## 기타 개선 아이디어

- 캘린더 뷰가 현재 "전체 업무"를 한 번에 불러옵니다. 업무 수가 많아지면
  `/api/tasks?from=&to=` 범위 쿼리(이미 API에는 구현되어 있음)를 캘린더
  페이지에서 실제로 사용하도록 바꾸는 것이 좋습니다.
- 태그가 단순 comma-separated 문자열입니다. 태그별 필터/자동완성이
  필요해지면 별도 `Tag` 모델로 정규화하는 것을 고려하세요.
- 드래그 앤 드롭으로 캘린더에서 일정 이동/리사이즈하는 인터랙션은
  아직 없습니다.
